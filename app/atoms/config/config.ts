declare const Config = {
    websocketConfig: {
        url: 'ws://localhost',
        params: {
            timeout: 3000,
            allowCellular: true
        },
        code:{
            noMoreOrders:'001'
        }
    }
};